plugins {
    id 'java-library'
    id 'war'
}

repositories {
    mavenCentral()
}

dependencies {
    String jakartaEeVersion = "8.0.0"
    providedCompile "jakarta.platform:jakarta.jakartaee-api:${jakartaEeVersion}"
}
